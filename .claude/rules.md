# AIEGIS MODULE — WORKSPACE RULES (Claude)

Source of truth:
- `../AI_RULES.md`

This file intentionally stays small to avoid duplication. When working in `aiegis-module/`, follow `AI_RULES.md` for workspace-specific rules.
